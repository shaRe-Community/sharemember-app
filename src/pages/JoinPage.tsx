/** /join — Join community via invite code (SM-4nc) */
export function JoinPage(): JSX.Element {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Join a Community</h1>
      <p>Enter an invite code to join a community.</p>
    </div>
  )
}
